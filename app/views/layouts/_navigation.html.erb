<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Tibbidy Swiggidy", root_path, class: "navbar-brand" %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if logged_in? %>
        <li> <%= link_to "Create a new recipe", new_recipe_path %></li>
        <li> <%= link_to "View profile", chef_path(session[:chef_id]) %></li>
        <li> <%= link_to "Logout", logout_path %></li>
        
        <% else %>
        <li><%= link_to "Login", login_path %></li>
        <% end %>
        
        
      </ul>
      <% if logged_in? %>
        <h4 class = "pull-right"> Logged in as <%= Chef.find(session[:chef_id]).chefname %> </h4>
        <% end %>
      
      
      
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>